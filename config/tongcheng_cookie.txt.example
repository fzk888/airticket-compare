# 同程 Cookie 示例文件
#
# 使用方法：复制本文件为同目录下的 tongcheng_cookie.txt，替换为真实 Cookie。
#   cp config/tongcheng_cookie.txt.example config/tongcheng_cookie.txt
#
# Cookie 获取：浏览器登录 www.ly.com 后，从开发者工具 Network 中复制
# 任一请求的 Cookie 头（需覆盖 .ly.com / .www.ly.com / .elongstatic.com / .40017.cn），整行粘贴即可。
# 同程机票/酒店价格需要登录态，Cookie 是必需的。
#
# 真实的 tongcheng_cookie.txt 不会提交（见 .gitignore）。

# 携程 Cookie 示例文件
#
# 使用方法：复制本文件为同目录下的 ctrip_cookie.txt，替换为真实 Cookie。
#   cp config/ctrip_cookie.txt.example config/ctrip_cookie.txt
#
# Cookie 获取：浏览器登录 m.ctrip.com 后，从开发者工具 Network 中复制
# 任一请求的 Cookie 头（形如 "key1=val1; key2=val2; ..."），整行粘贴即可。
#
# 没有有效 Cookie 时，携程酒店爬虫仍可返回酒店列表，但价格字段为空。
# 真实的 ctrip_cookie.txt 不会提交（见 .gitignore）。

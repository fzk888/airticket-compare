# FlyAI CLI 配置示例
#
# FlyAI 是飞猪官方 API 的命令行客户端，飞猪 FlyAI 爬虫（机票 + 酒店）通过它取价。
# airticket 现有的 scrapers/fliggy.py 同样依赖此 CLI。
#
# 安装：npm i -g @fly-ai/flyai-cli
# 验证：flyai --help
#
# 使用方法：复制本文件为 .env（或在 shell 中 export），填入真实 key。
#   cp config/flyai.env.example .env
#
# 注意：FLEXAI_API_KEY 属于个人凭据，真实值不要提交到仓库。
#
# FlyAI CLI 可执行文件名（默认 flyai，如装在自定义路径可改）
FLYAI_BIN=flyai

# FlyAI API Key（npm 安装 flyai-cli 后，按其文档获取）
FLYAI_API_KEY=

# 飞猪 Cookie 示例文件
#
# 使用方法：复制本文件为同目录下的 fliggy_cookie.txt，替换为真实 Cookie。
#   cp config/fliggy_cookie.txt.example config/fliggy_cookie.txt
#
# Cookie 获取：浏览器登录 www.fliggy.com（或 m.fliggy.com）后，
# 从开发者工具 Network 中复制任一请求的 Cookie 头，整行粘贴即可。
# 飞猪机票/酒店价格需要登录态，Cookie 是必需的。
#
# 真实的 fliggy_cookie.txt 不会提交（见 .gitignore）。
